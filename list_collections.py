import os; import chromadb; persistent_dir = os.path.join(os.path.expanduser("~"), ".deepwiki", "chromadb"); print(f"ChromaDB directory: {persistent_dir}"); client = chromadb.PersistentClient(path=persistent_dir); collections = client.list_collections(); print(f"Total collections: {len(collections)}"); print("All collection names:"); [print(f"- {c.name}") for c in collections]
